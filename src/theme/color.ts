import { palette } from './palette';

export const color = {

  palette,
  /**
  We define a transparent helper to lighten the code and make it more readable, as the fourth argument of rgba is often looked out
   */
  transparent: "rgba(0, 0, 0, 0)",

}